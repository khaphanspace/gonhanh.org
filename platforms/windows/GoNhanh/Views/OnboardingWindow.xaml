<Window x:Class="GoNhanh.Views.OnboardingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chào mừng đến với Gõ Nhanh"
        Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Gõ Nhanh"
                       FontSize="28" FontWeight="Bold"
                       Foreground="{StaticResource PrimaryBrush}"/>
            <TextBlock Text="Bộ gõ tiếng Việt cho Windows"
                       FontSize="14" Margin="0,5,0,0"
                       Foreground="{StaticResource SubtleBrush}"/>
        </StackPanel>

        <!-- Content -->
        <StackPanel Grid.Row="1" Margin="0,30,0,30">
            <TextBlock Text="Chọn kiểu gõ:" FontWeight="SemiBold" Margin="0,0,0,10"/>

            <RadioButton x:Name="TelexRadio" Content="Telex (phổ biến nhất)"
                         IsChecked="True" Margin="0,5"/>
            <TextBlock Text="Ví dụ: vieejt → việt, Vieejt Nam → Việt Nam"
                       FontSize="12" Margin="20,0,0,10"
                       Foreground="{StaticResource SubtleBrush}"/>

            <RadioButton x:Name="VniRadio" Content="VNI (dùng số)"
                         Margin="0,10,0,5"/>
            <TextBlock Text="Ví dụ: vie6t5 → việt, Vie6t5 Nam → Việt Nam"
                       FontSize="12" Margin="20,0,0,10"
                       Foreground="{StaticResource SubtleBrush}"/>

            <CheckBox x:Name="LaunchAtLoginCheckbox"
                      Content="Khởi động cùng Windows"
                      IsChecked="True" Margin="0,20,0,0"/>
        </StackPanel>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Bắt đầu" Click="StartButton_Click"
                    Width="100" Height="35"
                    Background="{StaticResource PrimaryBrush}"
                    Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
